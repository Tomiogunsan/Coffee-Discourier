/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
  }
  
  module.exports = {
    nextConfig,
    images:{
        domains: ["images.unsplash.com"]
    }

  }